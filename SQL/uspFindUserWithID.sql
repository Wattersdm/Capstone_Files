USE PlanIt
GO

CREATE PROCEDURE uspFindUserWithID
	@UserID		AS INT
AS

SET NOCOUNT ON
SET XACT_ABORT ON

BEGIN TRANSACTION

	SELECT * FROM Users
	WHERE UserID = @UserID
	AND Active = 1

COMMIT TRANSACTION
GO